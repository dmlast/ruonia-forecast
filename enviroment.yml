name: ruonia-forecast
channels:
  - conda-forge
  - defaults         
dependencies:
  # --- runtime ---
  - python>=3.10
  - pandas>=2.2
  - pyarrow>=15.0
  - requests>=2.31
  - urllib3>=2.2
  - python-holidays>=0.25
  - openpyxl>=3.1
  - lxml>=5.1
  # --- notebooks / viz ---
  - jupyterlab
  - ipykernel
  # --- optional CLI helpers ---
  - tqdm
  # --- pip section (пустая, если всё ставим conda-пакетами) ---
  - pip:
      []
